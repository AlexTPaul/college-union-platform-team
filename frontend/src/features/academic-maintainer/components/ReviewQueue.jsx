import React from "react";
import { FileText, ShieldCheck, CheckCircle2, Flag, Clock3 } from "lucide-react";
import { materials, pending } from "../../../data/demo";
import { Card, PageHead } from "../../../components/common/PagePrimitives";
export default function ReviewQueue({notify}){return <div className="review-layout"><Card><div className="card-head"><div><h3>Pending academic uploads</h3><p>Only your assigned scope is shown.</p></div><span className="pill">3 pending</span></div>{pending.map((m,i)=><div className="review-row" key={i}><div className="file-icon sm"><FileText size={18}/></div><div className="row-main"><b>{m.title}</b><span>{m.dept} · {m.sem} · {m.subject} · {m.uploader} · {m.time}</span></div><button className="iconbtn" onClick={()=>notify("Demo: material preview opened")}><FileText size={17}/></button><button className="approve" onClick={()=>notify("Material approved in demo")}>Approve</button><button className="reject" onClick={()=>notify("Material rejected in demo")}>Reject</button></div>)}</Card><Card><h3>Maintainer rules</h3><ul className="rule-list"><li><CheckCircle2/> You can only moderate assigned departments/semesters.</li><li><ShieldCheck/> Your own uploads cannot be approved by you.</li><li><Flag/> Reports create a separate review case.</li><li><Clock3/> Every action is recorded in moderation history.</li></ul></Card></div>}