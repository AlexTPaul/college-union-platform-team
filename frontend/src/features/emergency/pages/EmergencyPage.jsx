import React from "react";
import { useOutletContext } from "react-router-dom";
import { CircleAlert, HeartPulse, Phone, ShieldAlert, ShieldCheck } from "lucide-react";
import { Card, PageHead } from "../../../components/common/PagePrimitives";
export default function Emergency(){ const {notify}=useOutletContext(); const contacts=[["Campus Security","1122",ShieldAlert],["Health Centre","1044",HeartPulse],["Ambulance","108",Phone],["Fire / Emergency","101",ShieldAlert]];return <><PageHead eyebrow="ONE-TAP ACCESS" title="Emergency Hub" desc="Verified campus emergency contacts. Use only when needed."/><div className="emergency-banner"><CircleAlert size={22}/><div><b>Emergency numbers are verified by the institution.</b><span>Last verification: Aug 28, 2026</span></div></div><div className="emergency-grid">{contacts.map(([n,num,I],i)=><Card key={i}><div className="em-icon"><I/></div><span className="eyebrow">VERIFIED CONTACT</span><h2>{n}</h2><p>Available 24/7</p><button className="call" onClick={()=>notify("Demo only: call action triggered for "+n)}><Phone size={17}/> Call {num}</button></Card>)}</div><p className="hint"><ShieldCheck size={14}/> This demo does not dispatch emergency services.</p></>}