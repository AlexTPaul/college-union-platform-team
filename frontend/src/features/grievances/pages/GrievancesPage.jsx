import React from "react";
import { useOutletContext } from "react-router-dom";
import { ClipboardList, CheckCircle2, Clock3 } from "lucide-react";
import { Card, PageHead } from "../../../components/common/PagePrimitives";
export default function Grievances(){ const {notify}=useOutletContext(); return <><PageHead eyebrow="STUDENT SUPPORT" title="Grievances" desc="Raise an issue and track what happens next." action={<button className="primary" onClick={()=>notify("Demo: grievance form opened")}>+ New grievance</button>}/><div className="grievance-layout"><Card><div className="card-head"><div><h3>My grievances</h3><p>Private to your account</p></div><span className="pill">2 active</span></div>{[["GRV-2026-0142","Hostel maintenance request","Under Review","2 days ago"],["GRV-2026-0118","Canteen feedback","Resolved","Aug 21"]].map((g,i)=><div className="grievance-row" key={i}><div className="g-icon"><ClipboardList size={18}/></div><div className="row-main"><b>{g[0]} · {g[1]}</b><span>Updated {g[3]}</span></div><span className={"status "+(g[2]==="Resolved"?"green":"amber")}>{g[2]}</span></div>)}</Card><Card><h3>How it works</h3><div className="timeline">{["Submitted","Acknowledged","Assigned","Under Review","Resolved"].map((x,i)=><div className={i<4?"done":""} key={x}><span>{i<4?<CheckCircle2 size={17}/>:<Clock3 size={17}/>}</span><b>{x}</b></div>)}</div><button className="outline full" onClick={()=>notify("Demo: grievance tracking opened")}>Track a grievance</button></Card></div></>}